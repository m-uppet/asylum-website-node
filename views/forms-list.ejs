<!DOCTYPE html>
<html lang=en>
<head>
    <title> Asylum 669</title>
    <meta charset=utf-8 name="viewport" content="width=device-width, initial-scale=1"/>
</head>
<body>

<h1><%= title %></h1>
<ul>
    <% formResponses.forEach(function(formResponse) { %>
        <li><%= formResponse.title %><br/><%= formResponse.body %></li>
    <% }); %>
</ul>